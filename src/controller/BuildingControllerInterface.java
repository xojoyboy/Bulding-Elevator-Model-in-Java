package controller;

/**
 * Interface for the BuildingController class, which controls the building and display.
 * It defines the go method, which starts the controller.
 */
public interface BuildingControllerInterface {
  void go();
}
